const buttons = document.querySelectorAll(".buy-btn");  buttons.forEach((button) => {      button.addEventListener("click", () => {          const card =         button.parentElement;          const product = {              image:             card.querySelector("img").src,              name:             card.querySelector("h3").innerText,              price:             card.querySelector("p").innerText          };          let cart =         JSON.parse(localStorage.getItem("cart")) || [];          cart.push(product);          localStorage.setItem(             "cart",             JSON.stringify(cart)         );          alert(product.name + " added to cart 🛒");      });  });  const darkModeBtn = document.getElementById("darkModeBtn");  if(darkModeBtn){      darkModeBtn.addEventListener("click", () => {          document.body.classList.toggle("dark-mode");      });  }  let loggedInUser = localStorage.getItem("loggedInUser");  let userText = document.getElementById("userText");  if(loggedInUser && userText){      userText.innerText =     "Welcome, " + loggedInUser;  }  function logout(){      localStorage.removeItem("loggedInUser");      location.reload();  }  const searchBar = document.querySelector(".search-bar");  if(searchBar){      searchBar.addEventListener("keyup", () => {          let keyword =         searchBar.value.toLowerCase();          let cards =         document.querySelectorAll(".card");          cards.forEach((card) => {              let productName =             card.querySelector("h3")             .innerText             .toLowerCase();              if(productName.includes(keyword)){                  card.style.display = "block";              }else{                  card.style.display = "none";              }          });      });  }